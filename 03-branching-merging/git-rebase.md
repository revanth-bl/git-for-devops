````markdown
# git rebase

The `git rebase` command moves or reapplies commits on top of another branch.

## Rebase a branch

```bash
git rebase main
```

Rebasing creates a cleaner commit history but should be used carefully on shared branches.
````
