````markdown
# git merge

The `git merge` command combines changes from one branch into another.

## Merge a branch

```bash
git merge feature-branch
```

This command integrates the commits from the specified branch into your current branch.
````
