````markdown
# git fetch

The `git fetch` command downloads the latest changes from the remote repository without merging them.

## Fetch changes

```bash
git fetch
```

You can review the changes before merging them into your local branch.
````
