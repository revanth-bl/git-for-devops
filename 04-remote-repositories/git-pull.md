````markdown
# git pull

The `git pull` command fetches and merges changes from the remote repository.

## Pull the latest changes

```bash
git pull origin main
```

Use this command regularly to keep your local repository up to date.
````
