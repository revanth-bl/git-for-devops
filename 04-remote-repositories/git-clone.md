````markdown
# git clone

The `git clone` command creates a local copy of a remote repository.

## Clone a repository

```bash
git clone https://github.com/username/repository.git
```

After cloning, navigate into the project:

```bash
cd repository
```

This downloads the complete project history to your local machine.
````
