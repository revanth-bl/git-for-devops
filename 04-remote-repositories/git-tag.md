````markdown
# git tag

Tags are used to mark important versions or releases.

## Create a tag

```bash
git tag v1.0
```

## List tags

```bash
git tag
```

## Push tags

```bash
git push origin --tags
```

Tags help identify release versions such as `v1.0`, `v2.0`, etc.
````
