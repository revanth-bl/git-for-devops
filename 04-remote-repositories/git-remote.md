````markdown
# git remote

The `git remote` command manages remote repository connections.

## View remotes

```bash
git remote -v
```

## Add a remote

```bash
git remote add origin https://github.com/username/repository.git
```

## Remove a remote

```bash
git remote remove origin
```
````
