````markdown
# git revert

The `git revert` command creates a new commit that undoes the changes from a previous commit.

## Revert a commit

```bash
git revert <commit-id>
```

Unlike `git reset`, this command preserves the project history.
````
