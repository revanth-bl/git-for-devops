````markdown
# git reflog

The `git reflog` command shows a history of changes made to the HEAD reference.

## View reflog

```bash
git reflog
```

It is useful for recovering lost commits or undoing mistakes.
````
