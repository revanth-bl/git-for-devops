````markdown
# git reset

The `git reset` command moves the current branch to a previous commit.

## Soft reset

```bash
git reset --soft HEAD~1
```

## Mixed reset

```bash
git reset HEAD~1
```

## Hard reset

```bash
git reset --hard HEAD~1
```

**Warning:** `--hard` permanently discards uncommitted changes.
````
