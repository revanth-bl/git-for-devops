````markdown
# .gitignore

The `.gitignore` file tells Git which files or folders to ignore.

## Example

```text
node_modules/
.env
*.log
*.tmp
dist/
```

Ignored files are not tracked or committed to the repository.
````
