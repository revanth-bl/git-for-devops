````markdown
# git diff

The `git diff` command shows the differences between files or commits.

## Show unstaged changes

```bash
git diff
```

## Show staged changes

```bash
git diff --staged
```

Use this command before committing to review your changes.
````
