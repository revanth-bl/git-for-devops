````markdown
# git mv

Rename or move a file using Git.

## Rename a file

```bash
git mv old-file.txt new-file.txt
```

## Move a file

```bash
git mv file.txt folder/
```

Git tracks the file rename or move automatically.
````
